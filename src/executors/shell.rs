//! Shell executor for running shell commands as agents.
//!
//! This module re-exports the shell executor from the service layer.
//! See `crate::service::shell` for the full implementation.

pub use crate::service::shell::*;
